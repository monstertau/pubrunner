name: CustomPubmed
version: 0.1
resources:
   custom:
      - PUBMED_CUSTOM:
         pmid_filepath: disease.txt
         format: bioc
run:
   - python ../CountWords/CountWords.py -i {IN:PUBMED_CUSTOM/%.xml} -o {OUT:OUTPUTDIR/%.tsv}
output: OUTPUTDIR
